#ADT Stack

#Push:-
.if top pointer is > length of array 
.print Overflow
.else print the message to Enter Value 
.input value to be pushed
.array.append(value) or array[index] = value
.top pointer = top pointer + 1

#Pop:-
.if top pointer = -1 
.print Underflow
.else top pointer - 1 
.print top pointer

#Traverse:-
.if length of array = 0
.Array Empty
.for length of array
.print array[x]

